#include<bits/stdc++.h>
using namespace std;
int main(){
     int n=sizeof(int);
     bool flag = sizeof(bool);
     char ch = sizeof(ch);
     cout<<n<<endl<<flag<<endl<<ch<<endl;
    return 0;
}